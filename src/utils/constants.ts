export const FRONTEND_URL = process?.env?.FRONTEND_URL ?? "http://localhost:5173"
export const BACKEND_URL = process?.env?.BACKEND_URL ?? "/api"  // ← Cambiar a relativo
export const PRINTSCRIPT_SERVICE_URL = process?.env?.PRINTSCRIPT_SERVICE_URL ?? "http://localhost:8082"
export const AUTH0_USERNAME = process?.env?.AUTH0_USERNAME ?? ""
export const AUTH0_PASSWORD = process?.env?.AUTH0_PASSWORD ?? ""
